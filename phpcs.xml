<?xml version="1.0"?>
<ruleset name="Zero Spam for WordPress">
	<description>Custom ruleset for Zero Spam for WordPress, based on 10up standards.</description>

	<!-- Use 10up's standard ruleset -->
	<rule ref="10up-Default" />

	<!-- Scan everything in the current directory -->
	<file>.</file>

	<!-- Exclude standard paths -->
	<exclude-pattern>/vendor/</exclude-pattern>
	<exclude-pattern>/node_modules/</exclude-pattern>
	<exclude-pattern>/tests/</exclude-pattern>

	<!-- Pass arguments -->
	<arg value="sp"/> <!-- Show progress and sniff names -->
	<arg name="colors"/>
	<arg name="extensions" value="php"/>
	<arg name="basepath" value="."/>

	<!-- Set text domain -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array" value="zero-spam"/>
		</properties>
	</rule>

	<!-- Set PHP version for compatibility checks -->
	<config name="testVersion" value="8.2-"/>
</ruleset>
